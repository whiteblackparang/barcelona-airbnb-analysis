# 바르셀로나 Airbnb 예약 전환율 분석
### 숙소·호스트 특성이 예약(점유)에 미치는 영향 분석 및 지표 검증 프로젝트

---

## 1. 프로젝트를 시작하게 된 배경

데이터 분석가로 지원을 준비하면서, "그냥 캐글 데이터로 그래프 몇 개 그린" 포트폴리오는 만들고 싶지 않았습니다. 실무에서는 깨끗한 데이터가 주어지지 않고, 원하는 지표가 데이터셋에 없는 경우가 훨씬 많다고 생각했습니다.

그래서 처음엔 Kaggle의 뉴욕 Airbnb 데이터로 "예약 전환율 개선"이라는 주제를 잡았는데, 실제로 열어보니 **Superhost 여부, 즉시예약 가능 여부 같은 핵심 컬럼이 아예 없다는 걸** 확인했습니다. 여기서 데이터셋을 바꾸기로 했습니다.

- Kaggle 데이터 → Inside Airbnb(공식 소스)로 전환
- 뉴욕 → 바르셀로나로 도시 변경 (2028년까지 단기임대 라이선스를 전면 폐지하겠다는 규제 이슈가 있어, "규제 환경이 비즈니스 지표에 미치는 영향"이라는 스토리를 붙이기 좋다고 판단)

이 판단 과정 자체가 이 프로젝트의 첫 번째 의사결정이었습니다.

---

## 2. 프로젝트 목적

- **비즈니스 질문**: 어떤 숙소 특성, 어떤 호스트 특성이 예약(점유)으로 이어지는가?
- **실무 목표**: 단순 상관관계 나열이 아니라, 실제 서비스 개선에 쓸 수 있는 액션 아이템을 도출한다.
- **데이터 역량 목표**: 데이터 수집 → 정제 → 지표 설계 → 검증 → 재분석까지, 실무에서 요구되는 전체 파이프라인을 처음부터 끝까지 직접 수행한다.

> 그래프 삽입 위치 예시: `outputs/q1_room_type_validated.png` (숙소 타입별 점유율 비교)

---

## 3. 데이터 수집 (1단계)

| 항목 | 내용 |
|---|---|
| 출처 | Inside Airbnb 공식 사이트 (data.insideairbnb.com) |
| 대상 도시 | 스페인 바르셀로나 |
| 기준 시점 | 2026년 6월 24일 스크래핑본 |
| 받은 파일 | `listings.csv.gz`(상세 리스팅, 15,293건), `calendar.csv.gz`(날짜별 예약가능여부, 약 560만 행), `neighbourhoods.geojson` |

**실무 포인트**: Inside Airbnb는 요약본(visualisation용)과 상세본(전체 컬럼) 두 종류를 제공합니다. 처음에 요약본을 받았다면 Superhost, 리뷰 평점 같은 핵심 정보가 통째로 빠졌을 뻔했습니다. 목적에 맞는 파일을 고르는 것도 데이터 수집 단계의 중요한 판단이라는 걸 체감했습니다.

---

## 4. 데이터 처리 (2단계) — 정제와 지표 설계

원본 데이터엔 "예약이 몇 건 성사됐는지"를 알려주는 컬럼이 없었습니다. 그래서 직접 지표를 만들어야 했습니다.

### 4-1. 가격 데이터 정제
가격이 `"$409.00"`처럼 문자열로 들어있어 숫자로 변환하는 전처리가 필요했습니다.

### 4-2. 예약 프록시 지표 설계
`calendar.csv`의 날짜별 예약가능여부(`available`)를 이용해, 리스팅별로 "1년 중 며칠이 막혀있었는지"를 계산해 `occupancy_rate`(점유율)라는 지표를 직접 만들었습니다.

```python
cal['booked'] = (cal['available'] == 'f').astype(int)
occ = cal.groupby('listing_id').agg(
    total_days=('date', 'count'),
    booked_days=('booked', 'sum')
).reset_index()
occ['occupancy_rate'] = occ['booked_days'] / occ['total_days']
```

### 4-3. 결측치 처리 및 대체 지표 탐색
- `host_since`(호스트 가입일)가 전부 결측 → 대신 익명화된 `hosts_time_as_host_years`(호스트 경력 연차)로 대체
- `instant_bookable`(즉시예약 가능여부)이 100% 결측 → 이 지표는 분석 불가로 명확히 표시하고 넘어감 (있지도 않은 데이터를 억지로 추정하지 않는 것도 중요한 판단입니다)
- `host_response_rate`, `host_acceptance_rate`도 100% 결측 확인 → 대안으로 `license`(정식 등록번호) 필드를 발굴해 활용

> 그래프 삽입 위치 예시: 결측치 현황을 요약한 표 또는 막대그래프

---

## 5. 데이터 분석 (3단계) — 8개 질문에 대한 답

기획서 단계에서 정의한 8개 질문을 하나씩 검증했습니다. 실무에서 자주 쓰는 방법(상관분석, 구간별 비교, t-검정)을 적용했습니다.

| 질문 | 방법 | 결과 요약 |
|---|---|---|
| 가격이 예약에 영향을 주는가 | 상관분석 + 5분위 구간 비교 | 거의 무관 (r=-0.06) |
| 리뷰 수·평점이 영향을 주는가 | 상관분석 | 리뷰 수는 음의 상관(반직관적), 평점은 양의 상관 |
| Superhost 여부 영향은 | Welch's t-검정 | 통계적으로 유의미하게 높음 (p<0.0001) |
| 지역별 차이는 | 그룹별 평균 비교 | 지역 간 최대 2배 이상 차이 |
| 최소숙박일이 예약을 방해하는가 | 구간별 비교 | 단순 방해가 아니라, 4~14박 구간이 오히려 유리 |
| 라이선스(정식 등록) 여부 영향은 | Welch's t-검정 | 유의미한 차이 발견 (단, 5단계에서 재검증 필요했음) |

> 그래프 삽입 위치 예시: `outputs/q4_superhost.png`, `outputs/q6_min_nights.png`

---

## 6. 지표 검증 (4단계) — 이 프로젝트의 핵심 전환점

3단계까지 분석을 마친 뒤, 데이터에 Inside Airbnb가 자체적으로 제공하는 공식 추정 지표(`estimated_occupancy_l365d`)가 별도로 있다는 걸 확인했습니다. 제가 직접 만든 지표와 비교해봐야겠다는 생각이 들었습니다.

**결과: 두 지표의 상관계수는 -0.04로, 사실상 아무 관계가 없었습니다.**

더 확인해보니, 제가 직접 만든 `occupancy_rate`는 "예약된 날"과 "호스트가 개인 사정으로 막아둔 날"을 구분하지 못하는 구조적 결함이 있었습니다. 이 사실을 확인한 뒤, 앞서 3단계에서 냈던 결론 중 상당수를 다시 검증했습니다.

| 질문 | 처음 지표 기준 결론 | 검증된 지표로 재확인한 결론 |
|---|---|---|
| 어떤 숙소 타입이 예약이 잘 되는가 | Private room이 1위 | **Private room이 오히려 최하위** (완전히 반대) |
| 어떤 지역이 예약이 잘 되는가 | Les Corts가 1위 | Sants-Montjuïc이 1위로 변경 |

**이 과정에서 배운 것**: 스스로 설계한 지표라도 반드시 외부 벤치마크와 교차검증해야 하고, 결과가 "그럴듯해 보인다"는 이유로 검증 없이 결론을 내리면 실무에서 완전히 잘못된 의사결정으로 이어질 수 있다는 걸 직접 겪으며 배웠습니다.

> 그래프 삽입 위치 예시: `outputs/q1_room_type_validated.png` (기존 지표 vs 검증 지표 나란히 비교)

---

## 7. 프로젝트 성과 및 최종 인사이트

1. **Private room 카테고리는 재검토가 필요합니다.** 검증된 지표 기준 점유율이 가장 낮았고(연간 약 41일, 나머지 타입은 98~109일), 노출 전략이나 가격 전략을 다시 짜야 할 근거가 됩니다.
2. **지역 간 격차가 뚜렷합니다.** Nou Barris는 검증 후에도 확실한 최하위(연간 약 30일)로, 해당 지역 호스트 대상 지원책이 필요해 보입니다.
3. **가격은 예약 성사의 결정 요인이 아닙니다.** 가격을 낮춘다고 예약이 느는 것이 아니므로, 가격 할인보다 다른 레버(노출, 응답 속도 등)에 집중하는 게 효율적일 수 있습니다.
4. **가장 중요한 성과는 "지표 자체의 오류를 스스로 발견하고 바로잡은 것"입니다.** 실제 현업에서도 이런 지표 정합성 문제는 흔하고, 이를 놓치지 않는 것이 분석가의 핵심 역량이라고 생각합니다.

---

## 8. 이 프로젝트를 통해 기른 역량 (소프트 스킬 포함)

- **비판적 사고**: 처음 만든 지표를 맹신하지 않고, 스스로 의심하고 재검증한 태도
- **문제 정의 능력**: 원하는 데이터가 없을 때, 기획을 접는 대신 데이터셋을 바꾸고 대체 지표를 발굴하는 유연함
- **커뮤니케이션**: 분석 결과가 예상과 다르게(혹은 반대로) 나왔을 때, 이를 숨기지 않고 원인 가설을 함께 제시하는 정직한 리포팅
- **끈기**: 결측치, 스키마 변경(예: host_since 대신 익명화 필드로 대체), 지표 불일치 등 예상치 못한 문제를 만날 때마다 우회로를 찾아 끝까지 진행

---

## 9. 향후 업데이트 계획

- Superhost, 라이선스 여부에 대한 결론을 검증된 지표(`estimated_occupancy_l365d`) 기준으로 재확인
- `neighbourhoods.geojson`을 활용한 지역별 예약 히트맵 시각화 추가
- 파리 등 다른 도시 데이터와 비교 분석으로 확장
- 대시보드(Tableau/Metabase) 연동으로 실시간 모니터링 형태 구현

---

*이 프로젝트의 전체 코드와 노트북은 아래 구조로 정리되어 있습니다.*

```
barcelona-airbnb-analysis/
├── data/
├── notebooks/
│   ├── 01_occupancy.ipynb
│   ├── 02_clean_merge.ipynb
│   └── 03_eda_questions.ipynb
├── outputs/
└── report/
```